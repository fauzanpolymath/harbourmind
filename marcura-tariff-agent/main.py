# main.py
# Application entry point.
# TODO: Initialise the FastAPI app, register routers, configure middleware,
#       and set up startup/shutdown lifecycle hooks.
#
# Usage:
#   uvicorn main:app --host 0.0.0.0 --port 8000 --reload

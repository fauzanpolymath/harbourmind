# routes.py
# TODO: Define FastAPI APIRouter and HTTP endpoints here.
# Endpoints will include:
#   POST /calculate  - Accept port call details, return tariff calculation result
#   POST /parse      - Accept a raw tariff document, return parsed structured data
#   GET  /health     - Health check
